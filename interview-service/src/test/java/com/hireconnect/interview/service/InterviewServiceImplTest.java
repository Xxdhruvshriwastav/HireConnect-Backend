package com.hireconnect.interview.service;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder unit test for interview-service.
 * This ensures Maven always generates surefire-reports in CI.
 * No @SpringBootTest — no live DB or Eureka needed.
 */
class InterviewServiceImplTest {

    @Test
    void contextSanityCheck() {
        // Simple sanity test — no Spring context, no DB required.
        assertTrue(true, "Interview-service unit test placeholder passed.");
    }
}
